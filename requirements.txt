# Kept in sync with pyproject.toml [project.dependencies] — pyproject is
# canonical (uv sync); this file exists for plain-pip users only.
pygame>=2.6.1
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1
typing-extensions>=4.8.0
pillow>=12.2.0
